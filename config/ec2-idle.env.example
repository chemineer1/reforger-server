# Must match config/server.json rcon.password.
RCON_PASSWORD=change-me

# Keep the cost-saving default from the systemd unit: if the local player-count
# query fails continuously after startup grace, stop the instance anyway.
# Set to 0 if you prefer query failures to leave the EC2 instance running.
# IDLE_ON_QUERY_FAILURE=1
